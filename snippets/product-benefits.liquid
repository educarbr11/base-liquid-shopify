{%- comment -%}
  ============================================================
  product-benefits — bloco de benefícios configuráveis
  ============================================================

  O QUE FAZ:
  Exibe até 8 benefícios do produto em grid (imagem/SVG ou emoji + texto).
  Ícones, imagens e textos são configuráveis pelo Theme Editor.

  COMO USAR:
  {% render 'product-benefits', block: block %}

  PARÂMETROS:
  - block: bloco do schema contendo settings de cada benefício
{%- endcomment -%}

{%- assign benefit_count = 0 -%}
{%- for i in (1..8) -%}
  {%- assign label_key = 'benefit_' | append: i | append: '_label' -%}
  {%- assign label_val = block.settings[label_key] -%}
  {%- if label_val != blank -%}
    {%- assign benefit_count = benefit_count | plus: 1 -%}
  {%- endif -%}
{%- endfor -%}

{%- if benefit_count > 0 -%}
  <div class="pb" {{ block.shopify_attributes }}>
    {%- if block.settings.title != blank -%}
      <p class="pb__title">{{ block.settings.title | escape }}</p>
    {%- endif -%}
    <ul class="pb__list" role="list">
      {%- for i in (1..8) -%}
        {%- assign icon_key = 'benefit_' | append: i | append: '_icon' -%}
        {%- assign img_key = 'benefit_' | append: i | append: '_image' -%}
        {%- assign label_key = 'benefit_' | append: i | append: '_label' -%}
        {%- assign icon_val = block.settings[icon_key] | default: '' -%}
        {%- assign img_val = block.settings[img_key] -%}
        {%- assign label_val = block.settings[label_key] -%}
        {%- if label_val != blank -%}
          <li class="pb__item">
            {%- if img_val != blank -%}
              <span class="pb__icon pb__icon--media" aria-hidden="true">
                {{
                  img_val
                  | image_url: width: 96
                  | image_tag:
                    loading: 'lazy',
                    class: 'pb__img',
                    widths: '48, 72, 96',
                    alt: label_val
                }}
              </span>
            {%- elsif icon_val != blank -%}
              <span class="pb__icon" aria-hidden="true">{{ icon_val }}</span>
            {%- endif -%}
            <span class="pb__label">{{ label_val | escape }}</span>
          </li>
        {%- endif -%}
      {%- endfor -%}
    </ul>
  </div>
{%- endif -%}

<style>
  /* ── Product Benefits ── */
  .pb { margin: 20px 0; }
  .pb__title {
    font-size: var(--f1);
    font-weight: var(--font-heading-weight);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    opacity: 0.6;
    margin: 0 0 10px;
  }
  .pb__list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
    gap: 8px;
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .pb__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    padding: 10px 8px;
    background: var(--color-background-contrast);
    border-radius: var(--inputs-radius);
    text-align: center;
    border: 1px solid var(--color-border);
    transition: border-color 0.15s;
  }
  .pb__item:hover { border-color: var(--color-accent); }
  .pb__icon {
    font-size: 1.5rem;
    line-height: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
  }
  .pb__icon--media {
    font-size: 0;
  }
  .pb__img {
    display: block;
    width: 40px;
    height: 40px;
    object-fit: contain;
  }
  .pb__label { font-size: var(--f0); font-weight: var(--font-heading-weight); line-height: 1.3; }

  @media screen and (max-width: 600px) {
    .pb__list { grid-template-columns: repeat(3, 1fr); }
  }
</style>
